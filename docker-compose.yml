services:
  apaxy:
    image: antoniomedeiros/apaxy
    build:
      context: .
      args:
        apaxyPath: ''
    ports:
      - '8080:8080'
    volumes:
      - './share/:/usr/local/apache2/htdocs/share/'
